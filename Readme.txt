
Data.xlsx has raw and processed data.
 
Raw_data.mat is a Matlab workspace with the raw data

Raw_and_Stress_data.mat is a Matlab workspace with the raw and processed data

VMstress.m is a Matlab script which converts the raw data from Raw_data.mat into the processed (stress) data.